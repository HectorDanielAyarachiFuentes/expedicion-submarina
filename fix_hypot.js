const fs = require('fs'); const { execSync } = require('child_process'); const currentFile = fs.readFileSync('js/game/game.js', 'utf8').split('\n'); const originalFile = execSync('git show HEAD:js/game/game.js').toString('utf8').split('\n'); for (let i = 0; i < currentFile.length; i++) { if (currentFile[i].includes('Math.sqrt(()*() + ()*())')) { currentFile[i] = originalFile[i]; } } fs.writeFileSync('js/game/game.js', currentFile.join('\n'));
